-topic: t_mc_job_info_topic
  groupId: t_mc_job_info_group
  preprocesses:
    -className:
      params:
        -from:
          to:
  enable: true
  save:
    -type: mysql
      params:
        tableName: t_mc_job_info_realtime